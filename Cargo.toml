[package]
name = "rtouch"
version = "1.0.0"
edition = "2024"
License = "GPLv3-or-later"

[dependencies]
fs-err = "2.11.0"
dirs-next = "2.0.0"
clap = { version = "4.5.0", features = ["derive"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
