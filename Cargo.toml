[package]
name = "rtouch"
version = "1.2.1"
edition = "2024"
license = "GPLv3-or-later"


[dependencies]
clap = { version = "4.6.5", features = ["derive"] }
dirs-next = "2.0.0"
fs-err = "3.3.1"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
